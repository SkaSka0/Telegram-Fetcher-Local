# Virtual environment
.venv/

# Python cache
fetcher_core/__pycache__/
__pycache__/
*.pyc

# Pyrogram session (sensitif — berisi token login aktif)
*.session
*.session-journal

# Credentials & runtime state (sensitif)
fetcher_core/config/credentials.json
fetcher_core/config/user_state.json
config/user_state.json
.env

# Downloaded media (file besar, tidak perlu di-track)
downloads/
